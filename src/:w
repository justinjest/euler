#![allow(dead_code)]

mod utils;
mod largest_product_in_series;
use largest_product_in_series;
fn main() {
    let filename = "./resources/largest_product_in_a_series.txt";
    let ans = largest_product(filename, 4);
    println!("{ans}")
}
